($__TITLE__)

($import "stdlib.yu")
($import "h.yu")
($import "coroutine.yu")

($h-begin-named)

($coro-h)

($import "co-dict.yu")

($extern-c-begin)

($each-CORO \i.]
	($h-extern-init,,($coro-context ($i name)),,NULL)
	($h-extern-init,,int ($i name)_alive,,CO_SKIP)
	extern ($coro-declare ($i name));
	extern int ($i name)_init( void );
	extern void ($i name)_uninit( void );

[ )

($extern-c-end)

($h-end)
