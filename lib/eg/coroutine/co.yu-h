($__TITLE__)

($import "h.yu")
($h-begin-named)

($extern-c-begin)

($import "coroutine.yu")

($coroute-h)

($import "dict.yu")
($import "co-dict.yu")

($each-COROUTE \i.]
	($h-extern-init,,($coroute-context ($i name)),,NULL)
	($h-extern-init,,int ($i name)_alive,,CO_SKIP)
	extern ($coroute-declare ($i name));
	extern int ($i name)_init( void );
	extern void ($i name)_uninit( void );

[ )

($extern-c-end)

($h-end)
