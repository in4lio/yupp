[pytest]
addopts = --strict-markers
filterwarnings = error
pythonpath = src
testpaths = tests
markers =
    integration: exercises filesystem or subprocess behavior
    packaging: builds or installs distribution artifacts
